import axios from "../../api/axios";

const TERM_URL = "/term";

const loadAll = () => {};

const load = () => {};

const handleAdd = (formData, { setSubmitting, resetForm }) => {};

const handleSave = () => {};

const handleDelete = () => {};
