<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome to Other side</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f4f4f4;
        }
        .response-container {
            text-align: center;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }
        img {
            max-width: 100px;
        }
    </style>
</head>
<body>
    <div class="response-container">
        <img id="responseImage" src="<%= code === 200 ? '/../img/success.png' : '/../img/failed.png' %>" alt="Status">
        <h1 id="responseMessage"><%= message1 %></h1>
        <p><%= message2 %></p>
    </div>
</body>
</html>
